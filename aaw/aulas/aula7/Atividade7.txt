AAW ATIVIDADE 7

VULNERABILIDADE 01 — A busca de clientes
GET /api/clientes/buscar?nome=...

Endpoint de busca usado pela tela de atendimento. O parâmetro nome vem direto da caixa de busca do site.

 1  [HttpGet("buscar")]
 2  public IActionResult Buscar(string nome)
 3  {
 4      var sql = "SELECT * FROM Clientes WHERE Nome = '"
 5                + nome + "'";
 6      var clientes = _db.Clientes.FromSqlRaw(sql).ToList();
 7      return Ok(clientes);
 8  }
Sua análise:

Qual é a falha?

Qual o dano possível em produção?

Como corrigir?

VULNERABILIDADE 02 — A consulta de faturas
GET /api/faturas/{id}

Endpoint usado pelo app para exibir a fatura do cartão. O usuário está autenticado quando chama esta rota.

 1  [HttpGet("{id}")]
 2  public IActionResult GetFatura(int id)
 3  {
 4      var fatura = _db.Faturas.Find(id);
 5      if (fatura == null) return NotFound();
 6      return Ok(fatura);
 7  }
Sua análise:

Qual é a falha?

Qual o dano possível em produção?

Como corrigir?

VULNERABILIDADE 03 — A configuração do servidor
Program.cs (roda igual em dev e em produção)

Trecho de inicialização da API, idêntico em todos os ambientes. Este arquivo está versionado no Git da empresa.

 1  public const string Conn =
 2      "Server=prod-db;Database=Banco;User=sa;" +
 3      "Password=Newton@2026!";
 4
 5  var app = WebApplication.CreateBuilder(args).Build();
 6  app.UseDeveloperExceptionPage();
 7  app.Run();
Sua análise:

Qual é a falha?

Qual o dano possível em produção?

Como corrigir?

VULNERABILIDADE 04 — A atualização de perfil
PUT /api/usuarios/{id}

Endpoint que o app chama quando o usuário edita o próprio perfil. O corpo da requisição é o JSON enviado pelo cliente.

 1  public class UsuarioUpdate
 2  {
 3      public string Nome  { get; set; }
 4      public string Email { get; set; }
 5      public string Role  { get; set; }   // "user" | "admin"
 6  }
 7
 8  [HttpPut("{id}")]
 9  public IActionResult Atualizar(int id, UsuarioUpdate dto)
10  {
11      _repo.AtualizarTudo(id, dto);
12      return NoContent();
13  }
Sua análise:

Qual é a falha?

Qual o dano possível em produção?

Como corrigir?

DESAFIO
Qual das 4 falhas um scanner automático de código teria MAIS dificuldade de encontrar? Por quê?




________________________________________________________________________________________________________________


RESPOSTAS

1-
1.1- A falha é justamente o parâmetro ir direto na caixa de busca, ou seja, acesso direto ao banco pela busca.
1.2- O possível dano é de um usuário mal intencionado alterar ou buscar dados do banco cujos não deveria ter acesso.
1.3 -



2-
2.1- O erro nessa parte é não existir a verificação de se a fatura é do usuário logado
2.2- O possível dano é de que um usuário X acesse faturas que não sejam do id X, ou seja, de usuários diferentes do dele mesmo
2.3- 


3-
3.1- A falha é o usuário e a senha serem visíveis no código.
3.2- O possível dano é de que algum usuário perceba a existência desse acesso no código e os utilizem para entrar como 'admin'
3.3-


4-
4.1- O erro nesse código é atrelar a ROLE na tabela usúario
4.2- O possível dano é de que o usuário consiga alterar a própria role para 'admin' fazendo com que ele tenha acesso a áreas indevidas
4.3-
